import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AuthViewModel Testes', () {
    test('Teste inicial provisório', () {
      // Quando criarmos o AuthViewModel, vamos colocar as regras aqui!
      expect(true, true);
    });
  });
}